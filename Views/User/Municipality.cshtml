@model IEnumerable<HLWebRole.HLServiceReference.UserDto>
@{
    ViewBag.Title = "Users " + ViewBag.KOMNAVN;
    Layout = "~/Views/Shared/_LayoutThreeColumn.cshtml";
}
@section GeoNavigationMenuLinks
{
    <h2>
        Users - @ViewBag.KOMNAVN Municipality | @Html.ActionLink("News", "Municipality", "News", new { id = @ViewBag.Municipality }, null)
        | @Html.ActionLink("Communities", "Municipality", "Community", new { id = @ViewBag.Municipality }, null)
        | @Html.ActionLink("Areas", "Municipality", "Area", new { id = @ViewBag.Municipality }, null)
    </h2>
}
@section GeoNavigationMenu
{
    @{Html.RenderAction("_MunicipalityMenu", "GeoNavigationMenu", new { municipality = @ViewBag.Municipality });}
}
<div id="wrapper-div">
    <div id="map-div">
        @{Html.RenderPartial("_MapUserList", Model);}
    </div>
</div>
@section ItemList
{
    <ul id="item-list">
        @{Html.RenderPartial("_UserList", Model);}
    </ul>
}
